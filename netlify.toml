[build]
command = "npm ci --include=dev && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20.19.0"
# Make sure devDependencies (vite, plugin-react) are installed
NPM_FLAGS = "--include=dev"
